'use client'

export function PipelineRemindersPanel({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ padding:'16px 16px 0', marginTop:12 }}>
      {children}
    </div>
  )
}
