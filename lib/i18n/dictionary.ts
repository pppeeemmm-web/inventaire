/**
 * EN/FR UI copy — modularized under `./dictionary/` (Rings A–C in `keys.ts` · iPhone SE field terminal).
 * Import from here: `import { dict, type DictKey } from '@/lib/i18n/dictionary'`.
 */
export { dict, type DictKey, type Lang, type Dictionary } from './dictionary/index'
